#include <iostream>
#include <vector>
#include "matrix.hpp"
using namespace std;

int main()
{
   std::vector<std::vector<double> > a, b;
   Matrix A = Matrix(2, 3);//, B(2,3);
   //A.addElements(a);
   //B.addElements(b);
   cout << "Hello World!" << endl;

   return 0;
}

